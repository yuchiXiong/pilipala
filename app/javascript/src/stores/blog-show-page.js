const defaultState = {};

const blogShowPage = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default blogShowPage;