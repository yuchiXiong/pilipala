<!DOCTYPE html>
<html lang="cn" class="dark">
<head>
  <title><%= yield :title %></title>
  <link rel="icon" href="/website.svg"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0,user-scalable=no"/>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= vite_client_tag %>
  <%= vite_javascript_tag 'application' %>
  <%= vite_stylesheet_tag 'application.scss' %>
</head>

<body id="<%= "#{controller_name}__#{action_name}" %>">

<%= render 'shared/global_header' %>
<%# <%= render 'shared/header' unless %w(users__show blogs__show).include? "#{controller_name}__#{action_name}" %>
<%= yield :slot %>

<div class="container">

  <%= render 'shared/notice' %>
  <div class="app">

    <div id="app_left" class="col-12 col-lg-9">
      <%= yield :left_slot %>
    </div>
    <div id="app_right" class="col-3 d-none d-lg-block">
      <%= yield :right_slot %>
    </div>
  </div>

</div>

<%= yield :custom_slot %>

<footer class="mastfoot mt-auto border-top">
  <div class="inner">
    <p class="mb-0"><a href="https://beian.miit.gov.cn/">鄂ICP备18022630号-2</a></p>
    <p class="mb-0">Design By <a href="https://github.com/yuchiXiong">@yuchiXiong</a>.</p>
  </div>
</footer>

</body>
</html>
